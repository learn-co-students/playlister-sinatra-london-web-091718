<%=params[:created]? "Successfully created song." : "" %>
<%=params[:updated]? "Successfully updated song." : "" %>
<h1><%=@song.name%></h1>
<div class="container">
  <h2>Artist: <a href="/artists/<%= @song.artist.slug %>"><%= @song.artist.name %></a></h2>
  <h3>Genre(s):</h3>
  <% @song.genres.each do |genre| %>
    <p><a href="/genres/<%=genre.slug%>"><%=genre.name%></a></p>
  <% end %>
</div>